{
  "name": "imageshow",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @imageshow/server",
    "clean": "node -e \"const fs=require('node:fs'); ['dist','packages/shared/dist','packages/server/dist','packages/web/dist','packages/web/dist-ts'].forEach((path)=>fs.rmSync(path,{recursive:true,force:true}))\"",
    "build": "npm run clean && npm run build -w @imageshow/shared && npm run build -w @imageshow/web && npm run build -w @imageshow/server",
    "check": "npm run build -w @imageshow/shared && npm --workspaces run check",
    "start": "npm run start -w @imageshow/server"
  },
  "engines": {
    "node": ">=24"
  }
}
