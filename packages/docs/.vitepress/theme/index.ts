import DefaultTheme from "vitepress/theme";
import "./custom.css";

// Extends VitePress's default theme with a small CSS override (see custom.css); no
// component changes, so every default layout and feature keeps working as-is.
export default DefaultTheme;
